import React, { useState } from 'react';
import api from '../../services/api';
import { Header } from './styles';

const Dashboard: React.FC = () => {
  return <Header>Pokemón Games</Header>;
};

export default Dashboard;
